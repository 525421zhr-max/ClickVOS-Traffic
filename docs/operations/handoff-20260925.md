# 2026-09-25 继续开发交接

本文件是 DEV-28 完成时的快照；接手时先检查 `git status`、最新提交和路线图，不把快照当成之后的最新状态。

## 项目与约束

- 仓库：`ClickVOS`；远程 `https://github.com/525421zhr-max/ClickVOS-Traffic.git`。
- 核心目标：可实际使用的交通视频交互分割/半自动标注工具，随后完成十人试用、一次云 GPU 部署和软著材料。
- 永久禁止使用此前被用户禁止的女生照片及任何衍生文件；只使用自有、明确授权或许可清楚的合适视频。
- 不虚构数据，不把功能测试、单序列成绩或候选筛选写成完整数据集评估；不要把 DEV 编号当作十二周进度。
- 用户已授权项目内继续开发和 GitHub 备份。下载来源或权限不清时先问；不默认购买算力、绕过下载限制或改动账号配置。
- 原始媒体、GT、掩码、预览及模型只在本地。公开仓库只备份代码、配置、文档与核对过的摘要。

## 刚完成的工作

先读 `docs/checkpoints/DEV-28-bmx-trees-occlusion-diagnostic.md` 和对应结果 JSON。

- 完整 `bmx-trees` 80 帧双目标联合推理，两对象各保存 80 张掩码。
- 自行车首帧 IoU=0.6427，未达事先约定的 0.7 门槛；后续 J/F 仅作失败诊断，不能列为通过成绩。
- 骑行者首帧 IoU=0.8785，中间 78 帧 J&F=0.8623，树干遮挡处仍有明显误差。
- 两对象始终非空，没有重新激活事件，无法据此估计保护规则误拦截率。
- 评价器新增输入检查和 void 标记处理；91 项测试通过，`crossing` 六份旧报告所有原字段回归一致。
- 本地结果：`outputs/tasks/davis-bmx-trees-two-objects-001/`；`bicycle-rider-preview.mp4` 左自行车、右骑行者，10 FPS 仅作复核播放。
- 这是独立骑行小径诊断，不算补齐城市混合交通非机动车真值。

## 下一步的小闭环

优先利用已发现的自行车轮圈/车架空隙误覆盖，验证用户能否通过背景负点改善首帧选择。先读现有 Web 首帧补点和同任务重新传播实现，避免重复造功能。

在新实验前冻结提示、运行预算、门槛和停止条件；保留 DEV-28 原结果不覆盖。先单帧验证，达标再决定是否传播，并把修正点击数记录下来。不要不加区分地跑完整 DAVIS，也不必等待 KITTI 才推进工具。

城市道路非机动车逐帧真值，以及真实连续空掩码后同一目标重现，仍是素材缺口。BDD 主站与官方列出的 ETH 镜像在当前网络下访问失败；未下载数据。Docker 实机、云端、十人试用和软著提交仍待完成。

## 运行环境

在 Windows 项目根目录启动 WSL `Ubuntu-24.04`：

```powershell
wsl -d Ubuntu-24.04 -- /home/clickvos/.venvs/clickvos/bin/python -m pytest -q
wsl -d Ubuntu-24.04 -- /home/clickvos/.venvs/clickvos/bin/python scripts/check_repository.py
```

模型和评价环境的完整路径、源码提交、权重哈希与命令均在 DEV-28；不需要重新安装 SAM2。可选 `_C` 扩展未启用，目前核心推理可用。对新实验先做静态检查和小样本验证，不擅自扩成昂贵全量任务。
