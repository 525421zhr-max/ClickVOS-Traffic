# 周进度记录：W10 异常帧定位、候选确认与重叠提示

- 实施日期：2026-09-18
- 对应检查点：CP-06 异常提示与人工复核闭环
- 状态：工程实现和 CP-02 重新激活回归完成；重叠规则缺少真实阳性案例

## 实际完成

- Web 根据运行报告生成“目标—异常类型—帧号”下拉列表。
- 支持一键切换目标并载入异常帧，不再要求用户从 JSON 手工查找帧号。
- 对被重新激活保护暂停的候选，增加“确认候选目标重新出现”。
- 用户确认后恢复该对象从触发帧开始的候选掩码，重建合成预览，并记录确认对象、触发帧和恢复帧号。
- 已确认的重新激活保留在对象审计记录中，状态改为 `confirmed`，不再计入待复核异常数。
- 增加多对象掩码重叠检测；同一帧两个实例重叠达到 10 像素时标为待复核。
- 修复运行报告引用运行时字典导致旧报告被后续操作改写的问题，报告现在是独立快照。

## 可复现命令

```bash
/home/clickvos/.venvs/clickvos/bin/python -m pytest -q

/home/clickvos/.venvs/clickvos/bin/python scripts/verify_anomaly_review.py \
  --video data/processed/cp02/traffic-sample.mp4 \
  --checkpoint /home/clickvos/sam2.1-hiera-tiny-modelscope-20260914/sam2.1_hiera_tiny.pt \
  --output outputs/tasks/w10-anomaly-review-summary.json
```

## CP-02 真实 GPU 回归

输入仍为已登记的 `traffic-001`，首帧车辆正点 `(520,370)`，开启最大连通区域和重新激活保护。

| 检查项 | 实际值 |
| --- | ---: |
| 异常列表首项 | `1:46:reactivation_after_disappearance` |
| 确认前待复核异常 | 5 |
| 确认前第 46 帧最终像素 | 0 |
| 确认后第 46 帧最终像素 | 1533 |
| 恢复候选帧 | 46、47、48、49 |
| 确认后待复核异常 | 4 |
| 自动化测试 | 32 passed in 5.61s |

待复核数从 5 降至 4，是因为第 46 帧重新激活被标为已确认；第 10、12、46、49 帧的多连通区域提示仍然保留。

必须说明：CP-02 第 46 帧已经人工判定为另一辆车。本轮点击“确认”的目的仅是验证软件能执行并记录确认操作，恢复的 1533 像素仍然是错误目标，不能作为质量改进结果。这也说明人工确认本身可能做错，审计记录必须保留。

## 多对象重叠扫描

对 W08 双车辆任务按 10 像素阈值扫描 50 帧，实际重叠事件为 0。该结果只表明此样本没有命中，不证明规则能覆盖所有串目标问题。重叠阳性路径已用合成掩码自动化测试验证：两个对象重叠 8 像素时，在测试阈值 1 下正确产生事件。

## 遇到的问题

第一次真实验证输出错误地显示“确认前第 46 帧为 1533”。检查发现初始报告引用运行时像素字典，确认操作把旧报告同步改写。磁盘保护掩码没有丢失，也没有虚构结果。修复为复制快照后重新运行，得到确认前 0、确认后 1533。

SAM2 `_C` 可选扩展和 WSL localhost/NAT 警告仍存在，但没有影响本轮退出码。

## 本地产物

- `outputs/tasks/w10-anomaly-review-summary.json`
- `outputs/tasks/3d53ac633474/result.json`
- `outputs/tasks/3d53ac633474/exports/preview.mp4`

以上运行产物被 Git 忽略。

## 下一步

- 使用真实遮挡后同一目标再次出现的公开视频，测试正确确认场景和误拦截率。
- 收集至少一个真实对象重叠阳性案例。
- 为确认操作增加撤销入口，降低误确认风险。
