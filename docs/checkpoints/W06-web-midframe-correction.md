# 周进度记录：W06 Web 中间帧修正闭环

- 实施日期：2026-09-17
- 对应检查点：CP-03 中间帧修正、CP-04 Web 修正闭环
- 状态：单对象修正闭环完成；重复漂移仍需用户继续修正

## 实际完成

- Web 新增修正帧号、载入帧、正负点、清空提示和“应用修正并重新传播”。
- 初始传播后保留 SAM2 推理状态，不重新加载模型即可从指定帧继续传播。
- 支持只使用负点修正已有对象；新对象初始提示仍必须包含正点。
- 新任务启动时释放旧会话并清理 CUDA 缓存，限制为本地单用户、单活动任务。
- 修正历史、提示点、即时掩码像素数、重新传播帧数和耗时写入 `result.json`。
- 修复“传播缓存覆盖即时修正掩码”的工程错误。

## 真实回归

使用已登记的 CP-02 公开视频和原始首帧正点 `(520,370)`。初始传播后，第 46 帧错误车辆掩码为 1,533 像素；在错误车辆内部添加负点 `(480,350)`。

第一次实现虽然记录了修正并重新传播 4 帧，但第 46 帧仍为 1,533 像素。检查发现代码忽略了 `add_new_points_or_box` 的即时修正掩码，随后被传播迭代器的缓存帧覆盖。修复后重新运行得到：

| 帧 | 修正后像素 | 连通区域 |
| --- | ---: | ---: |
| 46 | 0 | 0 |
| 47 | 1,743 | 1 |
| 48 | 1,915 | 1 |
| 49 | 1,916 | 2 |

- 第 46 帧负点即时抑制成功：1,533 → 0 像素。
- 从第 46 帧重新传播 4 帧用时 0.388963487 秒。
- 错误目标从第 47 帧再次出现，因此一次负点没有永久消除后续身份漂移。

## 接受结论

工程门通过：网页后端确实保留会话、应用修正、更新修正帧、重新传播、重建视频并记录历史。

质量门只部分通过：单帧错误掩码可被负点消除，但 CP-02 的相似车辆在下一帧再次被识别。不能宣称中间帧一次修正即可解决身份漂移；当前用户需要在再次出现的帧继续负点修正，后续研究需探索“目标离场后停止传播/锁定消失状态”。

## 自动化测试

`23 passed in 4.81s`。

## 本地产物

- `outputs/tasks/w06-web-correction-summary.json`
- `outputs/tasks/ec47bf8c6ea8/result.json`
- `outputs/tasks/ec47bf8c6ea8/exports/preview.mp4`

## 下一步

- 将“连续消失后重新激活”升级为可选的自动阻断策略：目标长时间为空后，新出现掩码先暂停并等待用户确认，不直接写入最终结果。
- Web 接入多对象管理和每个对象独立修正。
